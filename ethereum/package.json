{
  "name": "sandbox-ethereum",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "truffle test",
    "test:watch": "SHELL=/bin/sh chokidar 'contracts/**' 'test/**' -c 'npm run test' --initial --silent",
    "migrate": "truffle migrate",
    "console": "truffle console"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "truffle": "^4.1.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chokidar-cli": "^1.2.0"
  }
}
